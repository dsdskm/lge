# TaskFlow 어시스턴트 필수 테스트

화면: `tms/taskflows/:taskFlowId/canvas`
명령은 코드 블록이라 복사 아이콘으로 바로 복사된다. 팔레트 이름이 다르면 바꿔 쓴다.

가정 팔레트: MoveTo `도슨트 대기 장소` / Tts `1.인트로`, `작별 인사` / PlayMotion `thumb_up` / PlayFace `Love`, `Idle`, `Joy` / PlaySound `이동 음악`

---

## A. 노드 하나 (이름 해석)

**A1. 기본 추가** — 흐름 끝에 붙는다
```text
Love 얼굴 추가해줘
```

**A2. 접미어 차이** — "도슨트 대기 장소" 로 맞춰야 한다
```text
도슨트 대기로 이동해줘
```

**A3. Task 표현이 이름에 섞인 경우** — `1.인트로` 로 맞춰야 한다
```text
인트로 음성 추가해줘
```

**A4. 오타** — 가장 가까운 이름으로 맞춰야 한다
```text
parallel 노드에 puase 추가해줘
```

**A5. 이름이 동작 표현과 같은 경우** — `이동 음악` 이 끼어들면 안 된다
```text
도슨트 대기 장소로 이동해줘
```

## B. 위치

**B1. 자식으로 (하단/아래/자식/하위)**
```text
parallel 아래에 thumb_up 추가해줘
```

**B2. 다음 순서로**
```text
parallel 우측에 작별 인사 추가해줘
```

**B3. 순번 지목 + 여러 노드** — Task 가 다르면 모두 붙는다
```text
두번째 parallel 아래에 thumb_up, 도슨트 대기, 작별 인사 추가해줘
```

**B4. Parallel 같은 Task 중복** — 하나만 붙고 나머지는 제외된다
```text
parallel 아래에 Love, Idle, Joy 추가해줘
```

## C. 제어 노드 구성

**C1. 동시 실행 (접속어)**
```text
도슨트 대기 장소로 이동하면서 이동 음악 틀어줘
```

**C2. 동시 실행 (나열)**
```text
1.인트로 tts, thumb_up 모션, Love 얼굴을 동시에 수행하는 parallel을 만들어줘
```

**C3. 조건 분기** — 조건 → 성공 → 실패 순서
```text
thumb_up 모션 성공하면 Love 얼굴, 실패하면 Idle 얼굴 보이게 하는 ifThenElse 노드를 만들어줘
```

**C4. 반복 + 후속 동작** — Repeat 자식은 Love 하나, 이동은 Repeat 우측
```text
Love 얼굴 3회 반복하고 도슨트 대기 장소로 이동해줘
```

**C5. 지연 + 후속 동작** — Delay 자식은 Love 하나
```text
3초 기다렸다가 Love 얼굴 보여주고 작별 인사 발화해줘
```

**C6. 타임아웃**
```text
Love 얼굴 실행하고 3초 타임아웃 걸어줘
```

## D. 수정·삭제

**D1. 교체**
```text
Love를 Joy로 바꿔줘
```

**D2. 삭제** — 노드가 새로 생기면 안 된다
```text
Love 노드 지워줘
```

**D3. 동명 노드** — 순번을 물어봐야 한다 (Love 가 2개 이상일 때)
```text
Love 노드 지워줘
```

**D4. 속성 변경**
```text
Repeat 반복횟수 5로 바꿔줘
```

## E. 복합 문장

**E1. 순차 여러 단계** — 이어붙기만 하고 기존 노드는 그대로
```text
도슨트 환영 장소 이동해서 1.인트로 발화하고 도슨트 안내 장소로 이동해서 2.TV 구조도 설명1 발화 해줘. 그리고 작별 인사하고 도슨트 대기 장소로 돌아오게 해줘
```

**E2. 제어 노드 + 순차 혼합**
```text
Love 얼굴 동시에 실행하고 작별 인사 발화해줘
```

**E3. 한 문장에 수정 두 건**
```text
Repeat 반복횟수 5로 바꾸고 Delay 시간을 2초로 바꿔줘
```

---

## 확인 포인트

| 항목 | 기대 |
|---|---|
| 위치 미지정 추가 | Start 에서 이어지는 흐름의 끝(제어 노드면 그 노드) 우측 |
| 기존 노드 | 절대 변경/삭제되지 않는다 |
| 제어 노드에 추가 | 기본은 자식. `우측/뒤에/다음에` 라고 말한 경우만 다음 순서 |
| Parallel 자식 | 같은 Task 는 하나만. 제외한 노드는 응답에 밝힌다 |
| IfThenElse 자식 | 말한 순서대로 condition → success → failure |
| Repeat/Delay/Timeout 자식 | 그 절의 동작만. 뒤 절은 제어 노드 우측으로 |
| 값 | 3초→`delay_msec:3000`, 3회→`num_cycles:3` |
| 이름 해석 | 접미어 차이 → 부분 이름 → 오타 순으로 맞추고, 애매하면 되묻는다 |
